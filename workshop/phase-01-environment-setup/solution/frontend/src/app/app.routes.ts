import { Routes } from '@angular/router';

// Real feature routes (login, character creation, game console) are added
// starting in Phase 5 once those standalone components exist. For now this
// is an empty route table so the app bootstraps cleanly with just the root
// <router-outlet></router-outlet> and a blank page.
export const routes: Routes = [];
